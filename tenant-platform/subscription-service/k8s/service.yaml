apiVersion: v1
kind: Service
metadata:
  name: subscription-service
  labels:
    app: subscription-service
    version: 1.0.0
spec:
  selector:
    app: subscription-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
