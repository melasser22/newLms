<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Inherit dependency & plugin versions for Spring Boot -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.5</version>
    <relativePath/> <!-- always use the central parent -->
  </parent>

  <groupId>com.ejada.tenant</groupId>
  <artifactId>tenant-platform</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>tenant-platform</name>

  <properties>
    <java.version>21</java.version>
    <spring-boot.version>3.5.2</spring-boot.version>

    <!-- Internal artifacts versions (change to your real ones) -->
    <ejada.starters.version>1.0.0</ejada.starters.version>
    <ejada.shared.version>1.0.0</ejada.shared.version>
    <!-- libs not managed by Boot BOM still need versions -->
    <springdoc.version>2.6.0</springdoc.version>
    <flyway.version>10.16.0</flyway.version>
    <lombok.version>1.18.34</lombok.version>
  </properties>

  <!-- Pin internal company artifacts so children don't need versions -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.ejada</groupId>
        <artifactId>shared-bom</artifactId>
        <version>1.0.0</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.ejada</groupId>
        <artifactId>shared-starter</artifactId>
        <version>1.0.0</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
 <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
      <version>${flyway.version}</version>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
      <version>${flyway.version}</version>
    </dependency>
      <!-- Manage versions for internal modules so children can omit them -->
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>tenant-persistence</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>tenant-config</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>tenant-events</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>tenant-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>tenant-service</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>subscription-service</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>catalog-service</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>billing-service</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>policy-service</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.ejada.tenant</groupId>
        <artifactId>tenant-api</artifactId>
        <version>${project.version}</version>
      </dependency>
       <dependency>
      <groupId>com.ejada</groupId>
      <artifactId>starter-openapi</artifactId>
    </dependency>
     <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
     <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    </dependency>
    </dependencies>
  </dependencyManagement>

  <modules>
    <module>tenant-core</module>
    <module>tenant-events</module>
    <module>tenant-api</module>
    <module>catalog-service</module>
    <module>subscription-service</module>
    <module>billing-service</module>
    <module>tenant-persistence</module>
    <module>tenant-config</module>
    <module>tenant-service</module>
    <module>policy-service</module>
  </modules>

 <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.11.0</version>
          <configuration>
            <source>${java.version}</source>
            <target>${java.version}</target>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
          <executions>
            <execution>
              <goals>
                <goal>repackage</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
  <!-- Optional: corporate plugin mgmt goes here if needed -->
</project>
