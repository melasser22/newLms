apiVersion: v1
kind: Service
metadata:
  name: ejada-catalog
  labels:
    app: ejada-catalog
    version: 1.0.0
spec:
  selector:
    app: ejada-catalog
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
