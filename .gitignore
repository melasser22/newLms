# --- OS cruft ---
.DS_Store
Thumbs.db

# --- Java / JVM ---
*.class
*.log
*.ctxt
hs_err_pid*
replay_pid*

# --- Build outputs (multi-module safe) ---
target/
**/target/
build/
**/build/
out/
bin/

# --- Packaged artifacts (don’t commit built jars/wars) ---
*.jar
*.war
*.ear

# --- Maven ---
.mvn/timing.properties
# Keep the Maven wrapper JAR committed:
!.mvn/wrapper/maven-wrapper.jar
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.tag
release.properties

# --- Gradle ---
.gradle/
**/.gradle/
# Keep the Gradle wrapper JAR committed:
!gradle/wrapper/gradle-wrapper.jar

# --- IDEs ---
# IntelliJ
.idea/
*.iml
*.ipr
*.iws
# Eclipse / STS
.project
.classpath
.settings/
.springBeans
.sts4-cache/
.factorypath
# VS Code
.vscode/

# --- Spring Boot runtime bits ---
*.pid
logs/
log/
tmp/
*.out

# --- Tests & Coverage ---
surefire-reports/
failsafe-reports/
jacoco.exec
jacoco/*.exec
coverage/

# --- Databases (local/dev) ---
*.h2.db
*.mv.db
*.db
*.sqlite

# --- Node (if UI lives in the repo) ---
node_modules/
**/node_modules/
dist/
build-frontend/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

# --- Docker / Compose (local only) ---
.env
.env.local
docker-data/
docker-logs/

# --- K8s / Helm (local overrides & secrets) ---
**/*secret*.yaml
values.local.yaml
secrets/

# --- Optional: local-only Spring configs (keep templates committed) ---
application-local.properties
application-local.yml
application-*.local.yml
application-*.secret.yml
