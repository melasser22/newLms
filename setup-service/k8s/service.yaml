apiVersion: v1
kind: Service
metadata:
  name: ejada-setup
  labels:
    app: ejada-setup
    version: 1.0.0
spec:
  selector:
    app: ejada-setup
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
