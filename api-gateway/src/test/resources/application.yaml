jasypt:
  encryptor:
    password: test-secret

spring:
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
  config:
    import: "optional:configserver:"

gateway:
  routes:
    tenant:
      id: tenant-service
      uri: lb://tenant-service
      paths:
        - /api/v1/tenants/**
